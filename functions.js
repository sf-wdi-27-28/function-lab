// YOUR CODE HERE
sillySum(arr);
var array = [1,2,3,4,5];

function sillySum {
  var count = 0;
  for (var i = 0; i < arr.length; i++) {
    count += (i * arr);
  }
}
sillySum(arr);

isPrime(num)

function isPrime {
  if (num === 0 || num === 1) return true;
  else (num === 2) return false;
}

filter

var filter = function(arr, callback) {
  
}
